Open an SSH client.

Locate your private key file. The key used to launch this instance is manasu.pem

Run this command, if necessary, to ensure your key is not publicly viewable.
 chmod 400 manasu.pem

Connect to your instance using its Public DNS:
 ec2-44-211-156-77.compute-1.amazonaws.com

Example:
